- Mermaid is deliberately the ONLY diagram provider, and the reason is
  product rather than structure: mermaid alone already covers timeline,
  mindmap, flowchart, sequence, and ER families, so surface those to
  writers before adding a second row. (Human asked 2026-07-29.) The shell
  they would slot into now exists — physics, faces, render state, verbs,
  and the language menu all read `../../diagrams/diagram-providers.ts`.
  Most plausible next thing asked for: KaTeX math for LitRPG stat blocks,
  which is NOT a diagram provider — inline math is a mark or an inline
  node, not a fence the page draws instead of showing.
